$.ajax({
  url: "/tacos?q=<%= URI.encode_www_form_component(params[:q]) %>"
}).done(function( msg ) {
  console.log(msg);
  $('#suggested-resource').html( msg );
}).fail(function( xhr, textStatus ) {
  console.log(xhr);
});
