$.ajax({
  url: "/suggested_resources?q=<%= URI.encode_www_form_component(params[:q]) %>"
}).done(function( msg ) {
  console.log(msg);
  $('#suggested-resource').html( msg );
}).fail(function( xhr, textStatus ) {
  console.log(xhr);
});
